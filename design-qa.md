# 偏好设置布局验收

final result: passed

## 最终布局与交互

- 使用用户提供的 macOS 系统设置截图作为双栏布局参考；实际内容为 AgentBar 设置。
- 主菜单为通用、供应商；供应商下仅有 Codex、Claude 一级子菜单，每页包含显示、账号和使用统计。
- 原生初始宽度为960，最小宽度640，可手动调整尺寸；macOS平台配置与通用配置一致。
- HTML高度填满当前窗体可用高度。切换页面、内容变化和移动窗口均不根据HTML改变偏好设置窗口尺寸。
- 左侧菜单和右侧内容分别滚动；顶部导航和底部保存栏保持可见。
- 左栏不显示“设置仅保存在当前设备”。
- 支持搜索、前进/返回、跨页面保留草稿、统一保存和还原；使用真实账号数据，浏览器中显示账号读取限制。

## 本轮证据

- 使用Codex内置浏览器打开 http://127.0.0.1:1420/#settings。
- 960×760视口：供应商列表与Codex页面的布局高度均为760px，保存栏底边为760px，无脚本设置的内联高度。
- 960×260视口：布局高度260px、保存栏底边260px。右侧滚动到380px时左侧保持0；左侧滚动34px时右侧保持380px；document滚动始终为0。
- 页面已视觉检查，内容无重叠，控件和保存栏可访问。
- 已移除前端偏好设置自然高度测量与原生resize请求；App的通用高度同步对整个settings窗口禁用，包括加载/错误状态。
- 已删除原生settings高度缓存、按内容缩放与移动/跨屏重调路径，保留主托盘及统计窗口原有行为。

## 工程验证

- pnpm check通过：类型检查、Lint、226项前端测试、7项脚本测试、生产构建。
- Rust panel相关测试30/30通过；cargo fmt检查通过。
- 本轮交互与尺寸验证在浏览器视口完成，未做原生鼠标拖动/跨屏实测。

## macOS 左侧标题栏布局

- macOS偏好设置使用Overlay标题栏，隐藏原生标题文字，保留原生窗口按钮；通用平台配置不变。
- 左侧固定51px顶部空间容纳窗口按钮，菜单滚动区从此区域下方开始；右侧导航从窗口顶部开始，不再叠加独立原生标题栏。
- 左侧顶部空白及右侧导航背景可拖动；导航按钮不标记为拖动区域。拖动及标题栏双击权限仅授予settings窗口。
- 真实Tauri原生窗口临时只读探针返回：full_size_content=true、title_hidden=true、titlebar_transparent=true、close_button_hidden=false、inner=960×620、outer=960×620。确认无额外标题栏高度且原生关闭按钮保留；探针已移除。
- 本机AppKit标准按钮几何结合Tao定位实现验证trafficLightPosition={x:13,y:22}对应红色按钮中心约(20,24)逻辑点；未以截图测量真实Tauri按钮位置。
- 浏览器小视口复验：左侧新滚动层可独立滚动，document滚动为0，右侧导航top为0。
- pnpm check通过（226项前端测试、9项脚本测试）；cargo check通过。
- 未通过原生鼠标操作验证拖动和双击；上述原生验证为真实窗口属性读取。
