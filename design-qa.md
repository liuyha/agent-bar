# AgentBar 配色验收

final result: passed

本次范围为 Codex 风格配色适配，保留 AgentBar 的布局、文字、图标形状和业务行为。没有按 Codex 聊天页面重建用量面板。

## 参考与截图

- 官方视觉来源：https://learn.chatgpt.com/docs/app
- 官方应用画面：https://learn.chatgpt.com/images/codex/video-posters/proactive-teammate-v2.webp
- 本地参考：`docs/screenshots/codex-color-reference.png`，1280 × 720 像素。
- 实现地址：http://127.0.0.1:1420/
- 用量页：`docs/screenshots/agentbar-light.png`、`docs/screenshots/agentbar-dark.png`。
- 设置页：`docs/screenshots/agentbar-settings-light.png`、`docs/screenshots/agentbar-settings-dark.png`。
- 实现截图为 1237 × 960 像素，对应 1237 × 960 CSS 视口；保存截图未缩放。参考与实现是不同产品画面，本次只比较白/灰底色、文字层次、边框和按钮反差，不进行布局叠图或声称像素级一致。官方参考为浅色，深色主题采用对应的中性灰层次。

## 对照结果

官方参考与浅色用量截图已在同一次视觉输入中打开对照；另检查浅色、深色设置页。

- 字体与文字：保留系统字体和现有字号、字重、行高；提高辅助文字对比度，已检查卡片数字、重置时间、提示及设置标签。
- 间距与布局：保留卡片、导航、控件尺寸和圆角；阴影改为较轻的中性黑。360 × 600 CSS 视口下，用量标签和重置时间无横向溢出。
- 配色：去除页面、深色面板、主按钮和进度条的绿调；浅色采用白/浅灰，深色采用 #212121、#262626、#2c2c2c 层次。Claude 图标保留小面积暖色，演示提示改为中性灰；警告及错误使用独立语义色。
- 图像与图标：保留已有图标形状与图标库，仅将浏览器图标改为黑白；没有新增位图资产进入产品界面。
- 文案与内容：未修改产品文案、演示标识、用量值或交互规则。

全图可辨识本次涉及的卡片、控件、标签与进度条，未另做区域裁剪；无需要通过局部放大判定的新图片资产。

## 修正与复核记录

1. 原有页面底层背景未随深色主题变化，截图底部可见浅色背景；统一 root/body 的页面背景变量。
2. 增加全视口背景高度后发现外边距折叠导致额外纵向滚动；为 body 增加 `display: flow-root`。最终常规视口 `scrollHeight = innerHeight = 960`，深浅色截图背景连续。
3. 已将组件中的固定品牌色、按钮色、进度色、阴影改为主题变量。删除 Claude 单独的进度色规则，保证高用量告警规则对两个服务都生效。

## 验证

- `pnpm build` 通过。
- 已操作用量/设置导航、浅色及深色保存、跟随系统保存、刷新用量；保存后页面确认“设置已保存并应用”。
- 系统主题验证：浅色系统时面板为 `rgb(255, 255, 255)`，模拟深色系统后自动切换为 `rgb(33, 33, 33)`；检查后已清除媒体及视口模拟并恢复预览原来的深色偏好。
- 浏览器控制台检查无 warning/error。
- 静态复核：所有颜色变量均有定义，显式深色和系统深色变量一致；辅助文字对比度浅色最低 4.60:1、深色最低 5.11:1，主按钮对比度至少 13.20:1。
- 错误提示与高用量告警完成 CSS/对比度复核，未为此注入演示故障或修改用量数据。
- 本次未重新打包或启动安装版 Tauri 应用；浏览器窄视口检查不等同于原生窗口运行验收。

当前配色范围内没有未解决的 P0/P1/P2 问题。
